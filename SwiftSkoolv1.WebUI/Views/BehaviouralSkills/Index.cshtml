@model IEnumerable<SwiftSkoolv1.Domain.BehaviouralSkill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BehaviorSkillCategories.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SkillName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BehaviorSkillCategoryId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SkillName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BehaviouralSkillId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BehaviouralSkillId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BehaviouralSkillId })
        </td>
    </tr>
}

</table>
