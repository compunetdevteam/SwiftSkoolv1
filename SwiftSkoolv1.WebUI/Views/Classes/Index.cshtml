@using SwiftSkool.Services
@model IEnumerable<SwiftSkoolv1.Domain.Class>

@{
    ViewBag.Title = "Index";
}

<h2>List of Classes</h2>
@{
    var barModel = new List<DataPoint>();
    //Html.RenderAction("Area", "ChartTypes", new { model = ViewBag.MyModel });
}
<p>
    @Html.ActionLink("Create New", "Create", new {@class= "btn btn-primary btn-raised" })
</p>


<div class="tablecontainer" style="width: 90%; margin: 0 auto">
    <a class="popup" href="/Classes/Save/0" style="margin-bottom: 20px; margin-top: 20px">Add</a>
    <table id="Jdatatable" class="table table-striped table-hover">
        <thead>
        <tr>
            <th class="sort-alpha">School Class</th>
            <th class="sort-alpha">Class Type</th>
            <th class="sort-alpha">Class Name</th>
            <th class="sort-alpha">Full ClassName</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        </thead>
    </table>
</div>


@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SchoolName)
        </th>
        <th>
            Class Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClassType)
        </th>
        <th>
            Full Class Name
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SchoolName)
            </td>
            <td>                
                @Html.DisplayFor(modelItem => item.ClassName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClassType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullClassName)
            </td>
            <td>
                <span>
                    <a href="@Url.Action("Edit", new {id = item.ClassId, @class = "btn btn-success" })"><span class="md-mode-edit"></span> Edit</a>
                </span> |
                @Html.ActionLink("Edit", "Edit", new {id = item.ClassId}) |
           
                <span>
                    <a href="@Url.Action("Delete", new {id = item.ClassId, @class = "btn btn-success" })"><span class="md-delete"></span> Delete</a>
                </span>
                @Html.ActionLink("Delete", "Delete", new {id = item.ClassId})
            </td>
            @{
                // var listofSchool = Model.Count(x => x.SchoolName.Equals(""));
                var newModel = new DataPoint(item.ClassId, item.FullClassName);

                barModel.Add(newModel);
            }
        </tr>
    }

</table>*@
@*<div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
        @{
            //foreach (var item in ViewBag.Model)
            //{
            //    Html.RenderAction("Area", "ChartTypes", new { model =  item});  
            //}
           
            Html.RenderAction("Area", "ChartTypes", new { model = barModel });
        } 
    </div>
    <div class="col-md-3"></div>
</div>*@


<script src="~/Scripts/jquery-3.1.0.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>

<script type="text/javascript">
    $(document).ready(function() {
        //debugger;
        //$("#Jdatatable").DataTable({
        //    "processing": true, // for show progress bar while loading
        //    "serverSide": true, // if it is true the preocess is done on server side
        //    "filter": false, // disables search box
        //    "orderMulti": false, // disable multiple column loading at once
        //    "ajax": {
        //        "url": "/Sessions/GetIndex",
        //        "type": "POST",
        //        "datatype": "json"
        //    },
        //var oTable = $("#Jdatatable").DataTable({
        //    // "processing": true, // for show progress bar while loading
        //    "ajax": {
        //        "url": "/Subjects/GetIndex",
        //        "type": "GET",
        //        "datatype": "json"
        //    },
        var oTable = $("#Jdatatable").DataTable({
            "processing": true, // for show progress bar while loading
            "serverSide": true, // if it is true the preocess is done on server side
            "filter": true, // disables search box
            "info": true,
            "stateSave": true,
            "orderMulti": false, // disable multiple column loading at once
            "ajax": {
                "url": "/Classes/GetIndex",
                "type": "POST",
                "datatype": "json"
            },
            "columns": [
                { "data": "SchoolName", "autoWidth": true },
                { "data": "ClassType", "autoWidth": true },
                { "data": "ClassName", "autoWidth": true },
                { "data": "FullClassName", "autoWidth": true },
               
                {
                    "data": "ClassId",
                    "autoWidth": true,
                    "render": function(data) {
                        return '<a class="popup" href="/classes/save/' + data + '"><span class="md-mode-edit"> Edit</a>';
                    }
                },
                {
                    "data": "ClassId",
                    "autoWidth": true,
                    "render": function(data) {
                        return '<a class="popup" href="/classes/delete/' + data + '"><span class="md-delete"></span> Delete</a>';
                    }
                }
                //{ "data": "EmailID", "autoWidth": true },
                //{ "data": "Phone", "autoWidth": true },
            ]
        });

        $('.tablecontainer').on('click',
            'a.popup',
            function(e) {
                e.preventDefault();
                OpenPopup($(this).attr('href'));
            });

        function OpenPopup(pageUrl) {
            var $pageContent = $('<div/>');
            $pageContent.load(pageUrl,
                function() {
                    $('#popupForm', $pageContent).removeData('validator');
                    $('#popupForm', $pageContent).removeData('unobtrusiveValidation');
                    $.validator.unobtrusive.parse('form');

                });

            $dialog = $('<div class="popupWindow" style="overflow:auto"></div>')
                .html($pageContent)
                .dialog({
                    draggable: false,
                    autoOpen: false,
                    resizable: true,
                    model: true,
                    title: 'Class Dialog Box',
                    height: 400,
                    width: 500,
                    close: function() {
                        $dialog.dialog('destroy').remove();
                    }
                });

            $('.popupWindow').on('submit',
                '#popupForm',
                function(e) {
                    var url = $('#popupForm')[0].action;
                    $.ajax({
                        type: "POST",
                        url: url,
                        data: $('#popupForm').serialize(),
                        success: function(data) {
                            if (data.status) {
                                $dialog.dialog('close');
                                oTable.ajax.reload();
                                alertify.success(data.message);
                            }
                        }
                    });

                    e.preventDefault();
                });
            $dialog.dialog('open');
        }
    });
</script>
