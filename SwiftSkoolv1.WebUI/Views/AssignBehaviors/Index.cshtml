@model IEnumerable<SwiftSkoolv1.Domain.AssignBehavior>

<style>
    span.field-validation-error {
        color: red;
    }
</style>



<div class="tablecontainer" style="width: 90%; margin: 0 auto">
    <a onclick="myFunction()" class="btn btn-primary btn-lg"> <i class="fa fa-plus left"></i> Add Subject</a>
    @*<a class="popup" href="/Subjects/Save/0" style="margin-bottom: 20px; margin-top: 20px">Add</a>*@
    <table id="Jdatatable" class="table table-striped table-hover">
        <thead>
            <tr>
                <th class="sort-alpha">Student Id</th>
                <th class="sort-alpha">Term Name</th>
                <th class="sort-alpha">Session Name</th>
                <th class="sort-alpha">Noo of Absence</th>
                <th class="sort-alpha">Behavioral Skill Id</th>
                <th class="sort-alpha">Skill Score</th>
                <th class="sort-alpha">Teacher Comment</th>
                <th>Actions</th>

            </tr>
        </thead>
    </table>
</div>
<!-- Modal -->
<div class="modal fade modal-bf-fh" id="actionsList" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <!--Content-->
        <div class="modal-content">
            <!--Header-->
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <div class="flex-center">
                    <h4 class="modal-title">Subject</h4>
                </div>
            </div>
            <!--Body-->
            <div class="modal-body" id="myModalDivBody">

            </div>
        </div>
        <!--/.Content-->
    </div>
</div>
<!-- /.Modal-->


@*@using (Html.BeginForm("Index", "AssignBehaviors", FormMethod.Get, new { @class = "form-inline" }))
{
    <!-- BEGIN INLINE FORM -->
    <div class="row">
        <div class="col-lg-7">
            <article class="margin-bottom-xxl">
                <p class="lead">
                    Please use the box below to search
                </p>
            </article>
        </div><!--end .col -->
        <div class="col-lg-12">
            <form class="form-inline">
                <div class="card">
                    <div class="card-body">
                        <div class="form-group">
                            @Html.DropDownList("StudentId", (SelectList)ViewBag.StudentId, "", new { @class = "form-control" })
                            <label for="StudentId">Student Id</label>
                        </div>

                        <div class="form-group">
                            @Html.DropDownList("TermName", (SelectList)ViewBag.TermName, "", new { @class = "form-control" })

                            @Html.DropDownList("SessionName", new SelectList(ViewBag.SessionName), "-select- ", new { @class = "form-control" })
                            <label for="TermName">Term Name</label>
                        </div>
                        <div class="form-group">
                            @Html.DropDownList("SessionName", (SelectList)ViewBag.SessionName, "", new { @class = "form-control" })

                           @Html.DropDownList("SessionName", new SelectList(ViewBag.SessionName), "-select- ", new { @class = "form-control" })
                            <label for="SessionName">SessionName</label>
                        </div>

                        <button type="submit" class="btn btn-raised btn-warning-light ink-reaction"><i class="fa fa-search"></i> Search</button>
                    </div><!--end .card-body -->
                </div><!--end .card -->

            </form>
        </div><!--end .col -->
    </div>

    <!--end .row -->
    <!-- END INLINE FORM -->
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TermName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SessionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NoOfAbsence)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BehaviorCategory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BehaviouralSkillId)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.SkillScore)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeacherComment)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TermName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SessionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NoOfAbsence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BehaviorCategory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BehaviouralSkillId)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.SkillScore)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeacherComment)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignBehaviorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignBehaviorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignBehaviorId })
            <span>
                <a href="@Url.Action("Edit", new {id = item.AssignBehaviorId, @class = "btn btn-success" })"><span class="md-mode-edit"></span> Edit</a>
            </span> |
            <span>
                <a href="@Url.Action("Delete", new {id = item.AssignBehaviorId, @class = "btn btn-success" })"><span class="md-delete"></span> Delete</a>
            </span>
        </td>
    </tr>
}

</table>*@



<script src="~/Scripts/jquery-3.1.1.min.js"></script>

<script type="text/javascript">

    $(document).ready(function () {




        window.oTable = $("#Jdatatable").DataTable({
            "processing": true, // for show progress bar while loading
            "serverSide": true, // if it is true the preocess is done on server side
            "filter": true, // disables search box
            "info": true,
            "stateSave": true,
            "orderMulti": false, // disable multiple column loading at once
            "ajax": {
                "url": "/AssignBehaviors/GetIndex",
                "type": "POST",
                "datatype": "json"
            },
            "columns": [
                { "data": "SubjectCode", "autoWidth": true },
                { "data": "SubjectName", "autoWidth": true },
                {
                    "data": "SubjectId",
                    "autoWidth": true,
                    "render": function (data) {
                        return '<a href="#" onclick="Edit(' + data + ')" ><i class="fa fa-pencil" aria-hidden="true" ><i/></a>' +
                            '<a class="text-danger" href="#" onclick="Delete(' + data + ')" ><i class="fa fa-remove" aria-hidden="true" ><i/></a>';

                    }
                }

            ]
        });

    });
    function Edit(subjecId) {
        var url = "/Subjects/Save?id=" + subjecId;

        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
    function Delete(subjecId) {
        var url = "/Subjects/Delete?id=" + subjecId;

        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
    function myFunction() {
        var url = "/Subjects/Save?id=0";


        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
</script>
